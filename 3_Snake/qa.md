# user question:

我想做 [貪食蛇遊戲]：

1. 主頁面
   1.1 遊戲簡介
   1.2 遊戲規則
   1.3 遊戲開始
   1.3.1 三種模式（簡單、中等、困難）
   1.3.2 選擇難度後進入遊戲畫面
   1.3.3 輸入玩家名稱
   1.4 分數排行
      1.4.1 分數會依照不同模式顯示
      1.4.2 分數從高到低排列
      1.4.3 分數會顯示玩家名稱
      1.4.4 分數會顯示遊戲時間

2. 遊戲畫面
   2.1 遊戲進行中
      2.1.1 蛇會移動
      2.1.2 蛇會吃食物
      2.1.3 蛇會變長
      2.1.4 蛇會撞到牆壁會結束遊戲
      2.1.5 蛇會撞到自己會結束遊戲
      2.1.6 蛇會撞到障礙物會結束遊戲
   2.2 遊戲結束
   2.3 遊戲暫停
   2.4 點擊 Logo 會返回主頁面

3. 架構
    3.1 html
    3.2 css
    3.3 javascript
        3.3.1 es6 class
        3.3.2 module
    3.4 node.js、vite


請幫我寫一個[markdown *.md檔]能夠達成此目標的『頂級提示詞』。在寫之前，請先問我 5~10 個必要的問題，以讓你能更了解以上的需求。


# AI answer:
為了幫你打造最頂級、滴水不漏的貪食蛇遊戲 `Prompt`，我們需要先釐清以下 **7 個關鍵的架構與設計細節**，以確保產出的 Prompt 能引導 AI 寫出具備工業級水準的程式碼：

### 1. 畫面渲染技術與碰撞解析度
你希望遊戲畫面使用 **HTML5 Canvas** 來繪製，還是使用 **純 DOM 節點（例如 Grid Layout / Absolute Positioning）**？
> *這會直接影響到「蛇的游動動畫流暢度（逐像素移動 vs 網格跳格移動）」以及「高幀率下的碰撞偵測精度」。*

### 2. 難度模式與障礙物機制
簡單、中等、困難三種模式的差異，除了**蛇的移動速度**之外：
* 障礙物是**固定關卡地圖（靜態障礙物）**，還是**隨機生成在場地中（動態障礙物）**？
* 難度會不會隨分數提高而自動增加（例如每吃 5 個食物速度變快，或多生出一個障礙物）？

### 3. 排行榜的資料持久化
分數與玩家名稱排行榜：
* 是否直接使用瀏覽器的 **`localStorage`** 進行本機儲存與排序？
* 還是需要預留 API 串接介面（例如設計一個 `ScoreRepository` 抽象類別，方便未來抽換成後端資料庫）？

### 4. 操控體驗與輸入緩衝（Input Buffering）
* 是否需要處理**「快速連續按鍵造成的反向自殺」**？（例如蛇往右走，玩家極速按下「下」再按「左」，若無輸入緩衝，蛇會在下一幀前直接判定往左撞到自己）。
* 是否需要支援行動裝置的**滑動手勢（Swipe）**或**虛擬搖桿**，還是僅限鍵盤 `WASD` / `Arrow Keys`？

### 5. 視覺美學風格
你預期這款遊戲的視覺完成度達到什麼等級？
* 例如：**現代 Glassmorphism（玻璃擬態 + 霓虹漸層 glow 效果）**、**Cyberpunk 暗黑霓虹風**，還是**復古 8-bit 像素風**？
* 蛇與食物是否需要粒子特效（例如吃掉食物時有爆炸碎屑散開）？

### 6. 音效與音樂系統
是否需要設計 **Web Audio API 聲效合成器**（免載入外部音檔，直接用程式碼合成 8-bit 吃食物、撞牆、死亡音效），還是暫不考慮聲音？

### 7. 系統架構與狀態機 (State Machine)
JavaScript 設計上是否需要使用**狀態模式（State Pattern）**來嚴格控管 `MENU`、`PLAYING`、`PAUSED`、`GAME_OVER` 之間的狀態轉換，防止在暫停時還能操作蛇移動等 Bug？

---
請告訴我你對這些細節的偏好，我會立刻為你量身訂做那份極致的 `Prompt` 模版！


# user answer:

1. 畫面渲染技術與碰撞解析度
    1.1 使用 SVG 來繪製。

2. 難度模式與障礙物機制
    2.1 簡單、中等、困難三種模式的差異，除了蛇的移動速度之外：
       地圖/速度：
       簡單：40x40，速度：100ms/step
       中等：60x60，速度：80ms/step
       困難：80x80，速度：60ms/step

3. 排行榜的資料持久化
    3.1 直接使用瀏覽器的 `localStorage` 進行本機儲存與排序。

4. 操控體驗與輸入緩衝（Input Buffering）
    3.1 需要處理「快速連續按鍵造成的反向自殺」。
    3.2 需要支援行動裝置的滑動手勢（Swipe）或虛擬搖桿。

5. 視覺美學風格
    3.1 現代 Glassmorphism（玻璃擬態 + 霓虹漸層 glow 效果）。
    3.2 蛇與食物需要粒子特效（例如吃掉食物時有爆炸碎屑散開）。

6. 有音效與無音樂
    6.1 使用 Web Audio API 聲效合成器（免載入外部音檔，直接用程式碼合成 8-bit 吃食物、撞牆、死亡音效）。

7. 有狀態機與無狀態機
    7.1 使用狀態模式（State Pattern）來嚴格控管 MENU、PLAYING、PAUSED、GAME_OVER 之間的狀態轉換，防止在暫停時還能操作蛇移動等 Bug。


